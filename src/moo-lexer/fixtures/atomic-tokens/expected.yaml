# BATCH_PHASE_PARAMETER NAME="R_MESSAGE_1" TYPE=UNICODE_STRING DIRECTION=INPUT
# {
#     ID=5
#     GROUP="Batch"
#     DESCRIPTION="Text Value Representing the First Part of the Prompt the Operator is to See"
# }

# There are 5 equal signs, one { and one }

keyword:
    - BATCH_PHASE_PARAMETER

property_name:
    - NAME
    - TYPE
    - DIRECTION
    - ID
    - GROUP
    - DESCRIPTION

quoted_string:
    - '"R_MESSAGE_1"'
    - '"Batch"'
    - '"Text Value Representing the First Part of the Prompt the Operator is to See"'

unquoted_string:
    - UNICODE_STRING
    - INPUT

number:
    - 5

# no comments, has equals, has quoted WS, lbraae and rbrace. has not errors.
